mutation updateUser(
  $name: String!,
  $title: String!,
  $isPublic: Boolean,
  $image: String!,
  $topics: [String]
) {
  updateUser(
    name: $name,
    title: $title,
    isPublic: $isPublic,
    image: $image,
    topics: $topics
  ) {
    name
  }
}
